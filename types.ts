import { IconType } from 'react-icons'

export type ServiceProps = {
    title: string
    about: string
    Icon: IconType
}

export type SkillProps = {
    name: string
    level: string
    Icon: IconType
}